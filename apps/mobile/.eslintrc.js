// https://docs.expo.dev/guides/using-eslint/
const { defineConfig } = require('eslint/config');
const expoConfig = require('eslint-config-expo/flat');

module.exports = defineConfig([
  // Global ignores so ESLint doesn't scan generated/bundled code
  {
    ignores: [
      'node_modules/**',
      'web-build/**',   // static export output
      '.expo/**',       // expo cache
      '**/_expo/**',    // nested _expo dirs inside web-build
      'dist/**',
      'expo-env.d.ts',  // generated by Expo
      '**/*.d.ts',      // generated typings
    ],
  },
  // Expo's recommended config (SDK 50)
  expoConfig,
]);
